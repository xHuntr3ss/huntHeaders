# huntHeaders
CLI tool to analyze HTTP security headers of a target URL. Detects missing, misconfigured, and deprecated headers based on OWASP recommendations (with summary, full, and per-header detail modes).
